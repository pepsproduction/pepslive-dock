{
  "name": "pepslive-dock",
  "private": true,
  "scripts": {
    "test": "npm run test:firebase:rules",
    "test:firebase:rules": "firebase emulators:exec --only database --project demo-pepslive-match-room \"node --test firebase/tests/database.rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.16.0",
    "firebase-tools": "15.24.0"
  }
}
